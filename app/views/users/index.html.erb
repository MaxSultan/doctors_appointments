<h1>Users</h1>
<%= link_to "Add User", new_user_path%>
<br/>
<h2><%= @users.length <= 0 ? "No Users" : ""%></h2>
<ul>    
    <% @users.each do |user|%>
        <li>
            <%= link_to user.first_name, user_path(user) %>
        </li>
   <% end %>

</ul>
 