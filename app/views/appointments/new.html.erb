<h1>New Appointment</h1>
<%= form_with(model: @Appointment, local: true) do |f|%>
<div class="field">
  <%= f.label :date_time %>
  <%= f.text_field :date_time %>

  <%= f.label :Doctor %>
  <%= f.selector :doctor_id, @doctors.collect  { |d| [d.full_name, d.id] }%>
  <%= end %>
